#include <iostream>

using namespace std;

int main() {

  char shirt_size;
  string color, name, surname;
  while (cin >> color >> shirt_size >> name >> surname) {
    if (color == "0")
      return 0;
  }
}
